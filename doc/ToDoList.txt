
Prioritized to do list:

- Tidy things up and check into ini GitHub
- Ask DaveC vacation schedule
- Assemble a zoo of test files that can be downloaded beyond the tiny ones in the repo 

- Split the Scalar model into String and Number
- change the Array model to be order-dependent somehow
- Add ability to override Models to an external program
- Create a template program that is quite simple as an example for a model creator
- Create a template program that wraps `file` for example as an external signature
- Create a generic file assessor, in binary mode, but recognize text files, tsv, xml, json,
- Improve the FileSignatures
- Package up as a nice Perl module
- Design and write a series of Perl t/ tests
- Expand the documentation

